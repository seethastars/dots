## Configuration file for CAVA.
# Remove the ; to change parameters.

[general]
mode = normal
framerate = 60
sensitivity = 60

bars = 0
bar_width = 5
bar_spacing = 1

[input]
method = pipewire
source = auto

sample_rate = 48000
sample_bits = 32
channels = 2
